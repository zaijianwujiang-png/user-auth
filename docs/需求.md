# 原始需求

## 这是个什么东西

一个简单的用户登录 / 注册系统。

## 主要用户是谁

普通终端用户（需要注册账号、登录使用的人）。

## 想要的核心功能

1. 用户注册：用邮箱 + 密码注册账号
2. 用户登录：用邮箱 + 密码登录，登录成功返回身份凭证（token）
3. 基本校验：邮箱格式校验、密码强度校验、重复邮箱不能注册
4. 查看当前登录用户信息（凭 token 获取自己的资料）

## 有什么限制 / 已知条件

- 先做最简单可用版本（MVP），不追求复杂功能
- 密码必须加密存储，不能明文
- 登录态用 token（如 JWT）
